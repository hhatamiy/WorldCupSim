{
  "buildCommand": "cd world-cup-sim && npm install && npm run build",
  "outputDirectory": "world-cup-sim/dist",
  "framework": "vite",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}

